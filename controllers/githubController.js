const {  getStarredRepositories } = require('../services/githubService');

const getStarredRepos = async (req, res) => {
//    Add logic here 
// Add Another function in github services
};

module.exports = { getGitHubRepos , getStarredRepos };
